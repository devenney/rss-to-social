# Copy this file to .dev.vars and fill in your values.
# .dev.vars is gitignored — never commit real credentials.
# Used by: npm run bootstrap, npm run nudge

RSS_FEED_URL=https://your-site.com/rss.xml
